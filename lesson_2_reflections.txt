1) What happens when you initalize a repository?
   Why do you need to do it?
1.1A) It creates a hidden directory .git
1.2A) git needs that for serveral things.

2) How is the staging area different from the working directory and 
   the repository? 
   What value do you think it offers?
2.1A) the staging area contains the changes which to commit
2.2A) Changes


3) How can you use the staging area to make sure you have one 
   commit per logical change?
3A) To add only these files which are contains the changes.


4) What are some situations when branches would be helpful in keeping 
   your history organized?
   How would branches help?
4.1A) for experimental features, or implement a new languages
4.2A) to have always a working master branch


5) How do the diagrams help you visualize the branch structure?
5A) I see faster the branches. But I am not sure with the easy-mode branch for now.


6) What is the result of merging two branches together?
   Why do we represent it in the diagram the way we do?
6.1A) The result is one branch.
6.2A) ???


7) What are the pros and cons of Git’s automatic merging vs. always 
   doing merges manually?
7A) merging manually is difficult because you have to find the changes yourself
    merging automatically the programm finds the changes for you
